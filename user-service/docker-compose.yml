services:
  user-service:
    build: .
    ports:
      - "3002:3002"
    depends_on:
      - mongo-user
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules

  mongo-user:
    image: mongo:8
    ports:
      - "27018:27017"