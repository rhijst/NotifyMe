version: '3.8'

services:
  task-service:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    depends_on:
      - mongo-task
    volumes:
    - ./:/app
    - /app/node_modules

  mongo-task:
    image: mongo:8
    ports:
      - "27017:27017"