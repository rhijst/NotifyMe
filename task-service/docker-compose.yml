version: '3.8'

services:
  task-service:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    depends_on:
      - mongo-task

  mongo-task:
    image: mongo:6
    ports:
      - "27017:27017"